\newcommand{\ccinput}[1]{
  \directlua{
    local chars = 0
    for line in io.lines('\luaescapestring{#1}') do
      tex.print(line)
      chars = chars + utf8.len(line)
    end
    tex.sprint(' ('..chars..'文字)')
  }
}
